stages:
  - setup
  - export
  - measure
  - plots
  - publish

variables:
  # Ort der virtuellen Umgebung
  VENV_PATH: "/data/gitlab/builds/runner/.venv"

setup-job:
  stage: setup
  script:

    - export PIP_CACHE_DIR=$CI_PROJECT_DIR/.cache/pip
    - mkdir -p $PIP_CACHE_DIR
    - python3 -m venv .venv
    - source .venv/bin/activate

    - export CUDA_ROOT=/usr/local/cuda
    - export CFLAGS="-I/usr/local/cuda/include"
    - export CXXFLAGS="-I/usr/local/cuda/include"
    - export C_INCLUDE_PATH=/usr/local/cuda/include
    - export CPLUS_INCLUDE_PATH=/usr/local/cuda/include
    - export LIBRARY_PATH=/usr/local/cuda/lib64
    - export PYTHONPATH=/usr/lib/python3.10/dist-packages:${CI_PROJECT_DIR}:$PYTHONPATH  

    - export LD_LIBRARY_PATH=/usr/lib/aarch64-linux-gnu:$LD_LIBRARY_PATH
    - ls -l /usr/local/cuda/include/cuda.h
    - pip install --upgrade pip
    - pip install torch==2.6.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/test/cu126 -q
    - pip install -r requirements.txt
    
    - python3 -c "import tensorrt; print(tensorrt.__version__)"
    - dvc pull --allow-missing    # damit nicht nach dvc dateien gesucht wird, die ich noch nicht gepusht habe
  tags:
    - jetson
    
export:
  stage: export
  script:
  # umgebungsvariablen für unterschiedliche Models
  # hier measure.export für alle skripte starten
    - python3 -m measure.export
  tags:
    - jetson

measure:
  stage: measure
  script:
  # umgebungsvariablen für unterschiedliche models und quantisierungsmethoden
    - python3 -m measure.mini_measure

tegrastats:
  stage: measure
  script:
  # umgebungsvariablen für unterschiedliche models und quantisierungsmethoden
    - python3 -m measure.tegrastats
  tags:
    - jetson

accuracy_comparison:
  stage: measure
  script:
    - python3 -m measure.accuracy_comparison
  tags:
    - jetson

plots:
  stage: plots
  script:
    - python3 -m plots.plots.py
  tags:
    - jetson

publish:
  stage: publish
  script:
    - dvc remote modify sciebo user $DVC_Sciebo_USER
    - dvc remote modify sciebo password $DVC_Sciebo_PASSWORD


    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
    - eval $(ssh-agent -s)
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" | base64 -d > ~/.ssh/id_ed25519
    - chmod 600 ~/.ssh/id_ed25519
    - ssh-keyscan github.com >> ~/.ssh/known_hosts

    - git config --global user.email "ci@example.com"
    - git config --global user.name "CI Runner"
    - dvc push -r sciebo  
    - dvc exp push -r sciebo -f git@github.com:iksnagreb/finn-transformers.git    # zweiter Teil funktioniert noch nicht


    #- git remote set-url origin https://oauth2:${GITLAB_PAT}@github.com:iksnagreb/finn-transformers.git
    #- dvc exp push -r sciebo -f https://oauth2:${GITLAB_PAT}@github.com/iksnagreb/finn-transformers.git   # klappt noch nicht...
  tags:
    - jetson

