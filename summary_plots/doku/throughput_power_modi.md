vision base/4 int8

generate a plot for througput comparison per power mode, each power mode should get its own line

def throughput_comparison_power_modes_plot(power_mode_files, output_path, value_key="throughput_images_per_s"):
in plots